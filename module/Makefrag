WORK = module
OBJDIRS += $(WORK)

$(OBJDIR)/$(WORK)/%.o: $(WORK)/%.c $(OBJDIR)/.vars.KERN_CFLAGS
	@echo + cc[MODULE] $<
	@mkdir -p $(@D)
	$(V)$(CC) -nostdinc $(KERN_CFLAGS) -c -o $@ $<
	$(V)$(OBJDUMP) -S $@ > $(patsubst %.o, %.asm, $@)
	$(V)$(NM) -n $@ > $(patsubst %.o, %.sym, $@)

